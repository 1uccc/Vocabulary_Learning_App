rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY TEST RULES - CHỈ DÙNG ĐỂ TEST
    // Cho phép tất cả user đã đăng nhập truy cập mọi data
    // SAU KHI TEST XONG, THAY BẰNG RULES CHÍNH THỨC
    allow read, write: if request.auth != null;
  }
}